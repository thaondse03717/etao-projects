(function($) {
	$.fn.autosave = function(options) {
		var opts = $.extend(this.options, options);
		
		this.options = {
			timeout: 4000,
			afterSave: function() {}
		};
		
		this.
	};
})(jQuery);
